meta {
  name: Generate Script
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/scripts/generate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "ideaId": "{{ideaId}}"
  }
}
