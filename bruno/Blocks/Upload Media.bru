meta {
  name: Upload Media
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/blocks/:id/upload
  body: multipartForm
  auth: bearer
}

params:path {
  id: <BLOCK_ID>
}

auth:bearer {
  token: {{token}}
}

body:multipart-form {
  file: @file()
}
